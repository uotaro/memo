# AIエージェント

1. [AIエージェント とは](#aiエージェントとは)
1. [CrewAI](#crewai)
1. [LangGraph](#langgraph)


## AIエージェントとは
一言で言うと「自分で考えて、自分で行動して、目標を達成するAI」。  
以下の３つの特徴がある。

| 特徴       | 説明                                       |
|------------|-------------------------------------------|
| 自律性     | 指示がなくても自分で次のアクションを決める     |
| ツール使用 | 外部のAPI、DB、ファイルシステム等を操作できる  |
| 目標指向   | 与えられたゴールに向かって行動し続ける         |

参考URL: [【入門】AIエージェントとは？仕組みから作り方まで徹底解説 #Python - Qiita](https://qiita.com/kotaro_ai_lab/items/d064882366936fc70550)

### エージェント開発の主要フレームワーク

| フレームワーク    |  特徴                                  | 言語                 |
|------------------|---------------------------------------|----------------------|
| Claude Agent SDK | Anthropic公式。Claude Codeと同じ基盤   | Python / TypeScript  |
| LangChain        | 最も有名。豊富なツール連携              | Python / TypeScript  |
| LangGraph        | LangChainの上に構築。複雑なワークフロー | Python               |
| CrewAI           | 複数エージェントの協調                  | Python               |
| AutoGen          | Microsoft製。マルチエージェント         | Python               |

## CrewAI

### CrewAI とは
- CrewAI は、複数のAIエージェントを協調させて複雑なタスクを自動化するための Pythonマルチエージェントフレームワーク。  
- 各エージェントに「役割（Role）」「目標（Goal）」「背景（Backstory）」を定義し、チームのように連携させる設計思想
- LangChainと統合可能で、ツール（Web検索・ファイル操作など）を各エージェントに持たせることができる
- 2023年後半に登場し、2024年にかけて急速に注目を集めたOSSプロジェクト

### CrewAI：参考URL
- [CrewAI 公式ドキュメント](#https://docs.crewai.com/)
- [CrewAI 使い方 — 調査メモ #Python - Qiita](https://qiita.com/nemoto_work_contact/items/374e97ddbd294841cb56)

### CrewAI：必要モジュールをインストール
python-dotenv は、環境変数として APIキーをセットしたくない場合入れる。環境変数を汚さず.env ファイルに書き込むだけでOKなので便利。

```bash
pip install crewai
pip install crewai[google-genai]   # Google Gemini API を利用する場合
pip install crewai[litellm]        # ローカルの Llama-cpp server を利用したい場合 
pip install python-dotenv          # GEMINI_API_KEY=[Google Gemini のAPIキー] セットのため
```

### サードパーティのツールを使いたい場合
ここでは例として SerperDevTool, ScrapeWebsiteTool を使ってみる。  
SerperDevTool, ScrapeWebsiteTool は、AI（LLM）にWeb上の最新情報を取ってこさせるための強力なツール。  
約2,500回分の Google 検索が無料枠として使える。  
これらのツールを使うには [https://serper.dev/](https://serper.dev/) でAPIキーを取得する必要がある。

| ツール名           | 説明                                                         |
|-------------------|--------------------------------------------------------------|
| SerperDevTool     | AI専用のGoogle検索エンジンプラグイン.                           |
| ScrapeWebsiteTool | 指定されたWebページの中身を丸ごと読み込むスクレイピングプラグイン. |

#### CrewAI：必要モジュールをインストール

```bash
pip install crewai[tools]          # ツール拡張など、機能込みこみでインストールする場合
```

#### CrewAI + llama.cpp の組み合わせで ScrapeWebsiteTool 利用時にエラーが出る場合
これは実は llama.cpp 側のOpenAI互換サーバーのバリデーションによるエラーで、CrewAI側の既知のバグが引き金になっている。  
具体的には、CrewAIのLLMクラス（crewai/llm.py の _format_messages_for_provider）には、メッセージ履行の末尾に assistant ロールが連続してしまった場合にそれを統合・修復する処理が入っているが、この特別な処理は Mistral と Ollama 系のモデルにしか適用されておらず、汎用的な llama.cpp / OpenAI互換エンドポイントには対応していない、という既知の不具合。  

- 参考URL: [CrewAI crashing with local llama.cpp - CrewAI Community Support - CrewAI](https://community.crewai.com/t/crewai-crashing-with-local-llama-cpp/6602)

##### 対応策：litellm.completion をラップして自動修復する
CrewAI本体を待たずに、自分のスクリプト側で連続assistantメッセージを統合してしまう

```python
#===================================================================
# litellm.completion をラップして自動修復 ※ llama.cpp APIサーバーを使う場合の一時的な対処
# litellm.completion() の呼び出し全体をフックするので、
# CrewAIの内部実装に依存せず、どのパスで連続assistantメッセージが発生しても通信前に修復できる。
#===================================================================
if use_local_llm:
    import litellm
    _original_completion = litellm.completion

    def _sanitize_messages(messages):
        """末尾に assistant ロールが2つ以上連続している場合、1つにマージする"""
        if not messages:
            return messages
        fixed = list(messages)
        while len(fixed) >= 2 and fixed[-1]["role"] == "assistant" and fixed[-2]["role"] == "assistant":
            merged = (fixed[-2].get("content") or "") + "\n" + (fixed[-1].get("content") or "")
            fixed[-2] = {**fixed[-2], "content": merged}
            fixed.pop()
        return fixed

    def _patched_completion(*args, **kwargs):
        if "messages" in kwargs:
            kwargs["messages"] = _sanitize_messages(kwargs["messages"])
        return _original_completion(*args, **kwargs)

    litellm.completion = _patched_completion
```

#### コンテキスト長エラーが出る場合
スクレイピングしたウェブページがLLMのトークン長を超える場合、エージェントがエラーで停止してしまうという報告がある。  
特に自マシンで 8080 など短いコンテキスト長でLLMを動かしている場合に頻出するエラー。

##### 対応策：ウェブページの結果をLLMの能力に応じて切り詰める
以下のようにウェブページの結果をLLMの能力に応じて切り詰めるとよい。  

```python
# コンテキスト長エラーが出る場合、スクレイピング結果を一定の長さで切り詰めるようにする
if use_local_llm:
    class TruncatedScrapeWebsiteTool(ScrapeWebsiteTool):
        max_chars: int = 3000  # 目安：日本語は1トークンあたり1～2文字程度なので、コンテキスト8080なら3000～4000字が安全圏

        def _run(self, **kwargs):
            result = super()._run(**kwargs)
            if isinstance(result, str) and len(result) > self.max_chars:
                return result[: self.max_chars] + "\n...(以下、文字数制限により省略)"
            return result
    # 使用箇所を差し替え
    scrape_tool = TruncatedScrapeWebsiteTool()
```

### CrewAI での実装サンプル
[uotaro/AI-agent_CrewAI: AIエージェントのお勉強：CrewAIフレームワークを使ってみる](https://github.com/uotaro/AI-agent_CrewAI)

---

## LangGraph

### LangGraph とは
LangGraph は、ワークフローをグラフ構造としてモデル化し、複雑なタスクの自動化、システムの状態管理を支援する。  
LangGraph は LangChain の一部として提供されている。
LangChain の Chains は一方向の処理フローを定義するが、LangGraph は循環的な処理フローを定義する。  
LangGraph を活用することでより複雑なシステムに対する処理フローを実現することが可能。 
その他の LangGraph の特徴は以下のとおり。

- 複数のエージェントが協力して問題を解決していくマルチエージェント開発に適している
- LangChain を基に開発されているため、LangChain の機能が活用できる
- グラフ構造を用いることで、「繰り返し処理」や「分岐処理」を実現できる

#### 参考URL
- [【初心者向け】LangGraphの紹介と基本的な使い方 #初心者 - Qiita](https://qiita.com/sakuraia/items/27db3f118e0ee41c54c1)
- [LangGraph 公式ドキュメント](https://docs.langchain.com/oss/python/langgraph/overview)
- [Workflows and agents - Docs by LangChain](https://docs.langchain.com/oss/python/langgraph/workflows-agents)

### LangGraph の主要な構成要素
LangGraphの主要な構成要素は、State、Node、Edge、Graphの4つがある。

| 構成要素  | 説明                                                                                                                           |
|----------|--------------------------------------------------------------------------------------------------------------------------------|
| State    | グラフに共通する状態を管理する要素。ワークフローで実行される各Nodeによって更新された値を保存する。                                     |
| Node     | 処理を行う要素。Stateから必要な情報を取り出し、処理結果をもとにStateを更新する。                                                     |
| Edge     | Node間を接続する要素。次に実行するノードを指定する。無条件に遷移するEdgeや条件に基づいて遷移先のNodeを決定する条件付きEdgeが設定できる。  |
| Graph    | NodeとEdgeで構成されるシステム全体を表す要素。                                                                                     |

### グラフ構造の可視化
LangGraphにはグラフ構造を可視化する機能が備わっている。  
この機能を活用することで、構築したチャットボットのグラフ構造を表示できる。  
グラフ構造の可視化のコードを実行する前に、以下2点を完了する必要がある。

- grandalf のインストール ( pip install grandalf )
- Graph のコンパイル

グラフ構造の可視化のコードは以下のとおり。

```python
print(graph.get_graph().print_ascii())
```

たとえば以下のように出力される.

```
        +-----------+        
        | __start__ |        
        +-----------+        
              *              
              *              
              *              
         +---------+         
         | chatbot |         
         +---------+         
          .        *         
        ..          **       
       .              *      
+---------+        +------+  
| __end__ |        | tool |  
+---------+        +------+  
```

### LangGraph での実装サンプル
[uotaro/AI-agent_LangGraph: AIエージェントのお勉強：LangGraphフレームワークを使ってみる](https://github.com/uotaro/AI-agent_LangGraph)
