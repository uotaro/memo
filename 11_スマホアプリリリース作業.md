# スマホアプリリリース作業

## フェーズ0: 今すぐ着手(承認待ちが発生するもの)
1. Apple Developer Program登録(¥12,800/年間) 
iPhoneにて、`Apple DEveloper` アプリをインストールして登録すると承認通りやすい＆費用が若干安いらしい。

2. Google Play Console登録(¥2500/初回のみ)


## フェーズ1: 登録の承認を待つ間に並行して進める
1. Android署名鍵の作成:  
keytoolで正式なアップロード鍵を作成し、key.propertiesで管理。  
アップロード作成コマンドは以下の通り（Android Studioがインストールされている前提）。

```bash
mkdir -p ~/keystores

"/Applications/Android Studio.app/Contents/jbr/Contents/Home/bin/keytool" -genkeypair -v \
  -keystore ~/keystores/inventory_manager_upload.jks \
  -alias upload \
  -keyalg RSA -keysize 2048 -validity 10000

# -keystore: 鍵ファイルの保存先。リポジトリの外(例: ~/keystores/)に置くことを推奨。誤ってgit add -A等でコミットされる事故を防げる。
# -alias upload: 鍵の識別名。任意の文字列でよいが、後で使うのでメモしておくこと。
# -validity 10000: 有効期限(約27年)。アプリの公開期間より長く設定するのがGoogle推奨。
```

**実行すると聞かれること**
- キーストアのパスワード(2回入力して確認) → 忘れないように保存
- 氏名・組織単位・組織名・都市・都道府県・国コード(国コードは日本ならJP) → ダミーでも実在情報でも可、証明書に埋め込まれるだけで公開されるものではない

**アップロード鍵を作成したら**
`プロジェクトルート/android/key.properties` を開き、以下2行に実際のパスワードに記述して保存。

```
storePassword=ここにキーストア作成時に入力したパスワードを設定
keyPassword=ここに鍵のパスワードを設定（キーストアと同じにした場合は同じ値）
```

保存できたら、動作確認として以下をターミナルで実行。

```bash
cd プロジェクトルードディレクトリパス
flutter build appbundle
```

成功すれば `build/app/outputs/bundle/release/app-release.aab` が生成され、正式なアップロード鍵で署名された状態になる。
念のため、下記コマンドで実際に正式な署名鍵で署名されているか(まだデバッグ鍵のままになっていないか)を確認する。

```bash
"/Applications/Android Studio.app/Contents/jbr/Contents/Home/bin/jarsigner" -verify -verbose -certs \
  build/app/outputs/bundle/release/app-release.aab | grep -A3 "X.509"
```

`CN=Android Debug, O=Android` ではなく、先ほど登録した `CN=登録した指名, OU=組織単位, O=組織名, L=都市, ST=都道府県, C=JP` が表示されればOK。

これでAndroid側は署名鍵の設定・リリースビルド成功まで完了。

2. ストア掲載素材の準備:  
スクリーンショット、アプリ説明文(短文・長文)、カテゴリ選定、サポート用連絡先など。

3. 実機での動作確認:  
特にカメラ・バーコードスキャン・写真選択はシミュレータ/エミュレータと実機で挙動が異なることがあるため、実機テスト必須。

## フェーズ2: 開発者登録が承認されたら
1. iOS署名設定:  
Xcodeで Apple Developer アカウントにログインし、証明書・プロビジョニングプロファイルを自動発行(Automatically manage signing)。

2. App Store Connect / Google Play Consoleでアプリレコードを新規作成

3. 各種アンケート回答:
    * Apple「Appのプライバシー」(データ収集なしの回答+カメラ/写真アクセスの利用目的申告)
    * Google「データセーフティ」フォーム+コンテンツレーティング(IARC)質問票
    * プライバシーポリシーURL(用意済み: https://uotaro.github.io/inventory_manager_privacy_policy/)を両方に入力
    * App Review 情報の「Memo（メモ・注記）」に以下を入力
       - 1. 実機での画面録画（Screen Recording）の提出 ... Youtubeの限定公開URLなど
       - 2. テストを行った端末とOSの一覧 ... 事前にテストを実施した端末とiOSのバージョンを記載
       - 3. アプリの機能と対象ユーザー ... このアプリがどのような課題を解決し、誰に向けたアプリなのかを簡潔に説明
       - 4. セットアップ手順とログイン情報（デモアカウント）... アプリを使うための設定手順や、ログインが必要な場合のテスト用アカウント情報を記載
       - 5. 使用している外部サービス・ツールの一覧 ... サードパーティのAPIやサービスを使っているかを記載
       - 6. 地域による機能差
       - 7. 規制業界・保護された第三者コンテンツについて

4. ビルドをアップロード(flutter build appbundle / flutter build ipa)

## フェーズ3: テスト・審査・公開
1. Google Play: クローズドテスト(20名・14日間)を実施 → 本番トラックへ昇格申請
2. Apple: TestFlightでの内部テスト(任意だが推奨) → App Store審査提出
3. 審査通過後、公開

## その他、見落としやすい残項目
* バージョン番号の確認:  
初回リリースは 1.0.0+1 でOKだが、以降の更新時はビルド番号(+1部分)を必ず上げる必要がある。
* プライバシーポリシーURLの作成:
プライバシーポリシー文言はAIに書いてもらおう。URLは、自身の `Github Pages` を使ってプライバシーポリシーを公開（他サイトでもOK）。
* 輸出コンプライアンス申告(Apple):  
暗号化技術の使用有無を回答。
* 利用規約(任意): ストア申請の必須項目ではないが、個人開発アプリでも用意しておくと後々のトラブル予防になるらしい。AIに書いてもらおう。

## iPhone バージョンアップリリース方法

1. ソース修正・ローカル環境でテスト
2. バージョン番号の更新 --- pubspec.yaml の `version:` 値を更新。 例）version: 2.0.0+4
3. 新バージョンの各種スクリーンショット画像を保存
4. Githubリポジトリに修正をプッシュ
5. Xcode で新バージョン（例えば v2.0.0(Build4)）をアーカイブ([後述](#xcodeでのアーカイブ手順))して、App Store Connect にアップロード
6. App Store Connect で新バージョン「2.0.0」を作成し、アップロード済みビルド(4)を紐付け
7. 「このバージョンの新機能」欄に更新内容を記載（買い物リスト機能追加、カテゴリー3階層化など）
8. 「App のプレビューとスクリーンショット」を新しいスクリーンショットに差し替え（6.9インチ/6.5インチなど必須サイズごとに登録。1サイズ分あれば残りは自動生成されることが多いが、確認推奨）
9. 「App Review情報」の「Memo（メモ・注記）」欄に内容([後述](#app-review-情報のmemoメモ注記に入力する内容))を記載
10. 「審査用に追加」ボタン → 「審査へ提出」ボタンクリックして審査へ

### App Review 情報の「Memo（メモ・注記）」に入力する内容
1. 実機での画面録画（Screen Recording）の提出 ... Youtubeの限定公開URLなど
2. テストを行った端末とOSの一覧 ... 事前にテストを実施した端末とiOSのバージョンを記載
3. アプリの機能と対象ユーザー ... このアプリがどのような課題を解決し、誰に向けたアプリなのかを簡潔に説明
4. セットアップ手順とログイン情報（デモアカウント）... アプリを使うための設定手順や、ログインが必要な場合のテスト用アカウント情報を記載
5. 使用している外部サービス・ツールの一覧 ... サードパーティのAPIやサービスを使っているかを記載
6. 地域による機能差
7. 規制業界・保護された第三者コンテンツについて

### Xcodeでのアーカイブ手順

1. 事前準備（ターミナル）

```bash
cd /Users/ctani/work/05_app/20260810_inventory_manager/03_src/my_inventory
flutter clean
flutter pub get
```

`flutter pub get` を実行すると `ios/Flutter/Generated.xcconfig` に `FLUTTER_BUILD_NAME=2.0.0` / `FLUTTER_BUILD_NUMBER=4` が書き込まれ、Xcode側のバージョン表示に反映される。

2. Xcodeでワークスペースを開く
必ず ios/Runner.xcworkspace を開くこと（Runner.xcodeprojではない）。

```
open ios/Runner.xcworkspace
```

3. デバイスを「Any iOS Device」に変更
Xcode上部のツールバー、スキーム名（Runner）の右にあるデバイス選択メニューをクリックし、「Any iOS Device (arm64)」 を選択する。

- シミュレータを選択したままだと、メニューの「Archive」がグレーアウトして選べないので注意。
- 実機を接続していればその実機を選んでも構わないが、通常はAny iOS Deviceで問題ない。

4. 署名設定の確認
左のナビゲータで Runner（プロジェクト）→ TARGETS の Runner → 「Signing & Capabilities」タブを開き、以下を確認する。

- Automatically manage signing：チェックON
- Team：想定しているものが選択されている
- Bundle Identifier：com.lemox.myInventory

「General」タブでは Version が 2.0.0、Build が 4 になっていることも確認すること（手順1を実施していれば自動反映されているはず）。

5. アーカイブ実行
メニューバーから `Product → Archive` を選択。

ビルドが走り（数分かかります）、成功すると自動的に Organizer ウィンドウが開き、作成したアーカイブが一覧に表示される。

6. Organizerからアップロード
Organizerで該当のアーカイブ（2.0.0 (4)）を選択し、

1. （任意）「Validate App」で事前チェック
2. 「Distribute App」をクリック
3. 配布方法で 「App Store Connect」 を選択 → 「Distibute」を選択（TestFlight/App Store提出両方に使える）
4. 署名オプションは「Automatically manage signing」のまま進める
5. 最終確認画面で「Upload」

アップロードが完了すると、App Store Connect側で 5〜30分程度の処理待ち（メール通知が届く）を経て、対象アプリの「TestFlightとビルド」にビルド4が表示されるようになる。  





## TODO: Android は後ほどまとめる